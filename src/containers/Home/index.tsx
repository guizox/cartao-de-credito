import React from "react";
import Container from "@material-ui/core/Container";

interface Props {}

const Home: React.FC<Props> = () => {
  return <Container maxWidth="xl">empty container</Container>;
};

export default Home;
